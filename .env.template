#FIREBASE SETUP
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""
VITE_FIREBASE_MEASUREMENT_ID=""

# CLOUD FUNCTIONS PARAMS (set in functions/.env for local dev; see functions/README.md)
# The email address that is allowed to call staff-provisioning callable functions
# (createStaffAccount, setStaffDisabled, deleteStaffAccount).
# In production, set this via the Firebase console → Functions → Configuration,
# or by running: firebase functions:params:set SUPERUSER_EMAIL=admin@example.com
SUPERUSER_EMAIL=""

# SUPERUSER EMAIL (client-side UX gate for the /staff management page)
# Must match SUPERUSER_EMAIL above. Real enforcement is server-side; this only
# controls whether the "Staff" nav link and /staff page are shown in the UI.
VITE_SUPERUSER_EMAIL=""